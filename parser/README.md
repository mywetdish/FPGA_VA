# Parser

Parser is a program for make two files: dut_wrapper to the board and copy of your module for verification

To launch parser run this command:

```bash
python3 init_script.py
```

Then you need to enter name of your file (example: riscv_unit.sv), and run:

```bash
python3 parser.py
```

That's it! All files are already in the required directories
