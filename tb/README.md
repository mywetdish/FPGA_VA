# Simple simulation manual
After parsing and loading bitstream to board, you need to reboot computer. To start simulation, go to /tb directory and run this command:

```bash
make connect
```

After success connection, start simulation by:

```bash
make
```

All your testbench files needed to be in /tb/src directory
